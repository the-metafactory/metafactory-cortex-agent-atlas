{
  "name": "metafactory-cortex-agent-atlas",
  "version": "0.1.0",
  "description": "Atlas — the Factory's product-manager agent (plan steward). ADD/REMOVE intake with a principal RATIFY gate, map+ledger atomic updates, self-healing reconcile, doc changes by PR only. Spec: the-metafactory/vision#9",
  "type": "module",
  "private": true,
  "license": "MIT",
  "repository": "github:the-metafactory/metafactory-cortex-agent-atlas",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "shadow": "ATLAS_SHADOW=1 bun test test/e2e/walkthrough.test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
